/* eslint-disable no-unused-vars */
export enum EventSpaceType {
  NONE = 'none',
  PARK = 'park',
  SIDEWALK = 'sidewalk',
  ROAD = 'road',
  YARD = 'yard',
}

export enum EventSpaceArea {
  NONE = 'none',
  XS = 'xs',
  SM = 'sm',
  MD = 'md',
  LG = 'lg',
  XL = 'xl',
}
